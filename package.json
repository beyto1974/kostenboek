{
  "name": "kostenboek",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.node.json",
    "verify": "npm run check && npm test && npm run e2e",
    "e2e": "playwright test"
  },
  "dependencies": {
    "solid-js": "^1.9.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^22.10.0",
    "fake-indexeddb": "^6.2.5",
    "typescript": "^5.7.2",
    "vite": "^6.0.3",
    "vite-plugin-solid": "^2.11.0",
    "vitest": "^4.1.11"
  },
  "license": "MIT",
  "description": "Local-first hour book: paint the hours you worked on a calendar, read what you are still owed",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/beyto1974/kostenboek.git"
  }
}
